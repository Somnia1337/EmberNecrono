```c
#include <stdio.h>

int main()
{
	long N, A;
	int a = 1, b = 1, c = 1, d = 1, e = 1, f = 1, g = 1;
	scanf("%ld", &N);
	switch (N)
		{
		case 3:
			A = 100;
			while(A < 1000)
			{
				a = A / 100;
				b = (A - a * 100) / 10;
				c = A % 10;
				if (a * a * a + b * b * b + c * c * c == A)
				{
					printf("%ld\n", A);
				}
				A++;
			}
			break;
		case 4:
			A = 1000;
			while (A < 10000)
			{
				a = A / 1000;
				b = (A - a * 1000) / 100;
				c = (A - a * 1000 - b * 100) / 10;
				d = A % 10;
				if (a * a * a * a + b * b * b * b + c * c * c * c + d * d * d * d == A)
				{
					printf("%ld\n", A);
				}
				A++;
			}
			break;
		case 5:
			A = 10000;
			while (A < 100000)
			{
				a = A / 10000;
				b = (A - a * 10000) / 1000;
				c = (A - a * 10000 - b * 1000) / 100;
				d = (A - a * 10000 - b * 1000 - c * 100) / 10;
				e = A % 10;
				if (a * a * a * a * a + b * b * b * b * b + c * c * c * c * c + d * d * d * d * d + e * e * e * e * e == A)
				{
					printf("%ld\n", A);
				}
				A++;
			}
			break;
		case 6:
			A = 100000;
			while (A < 1000000)
			{
				a = A / 100000;
				b = (A - a * 100000) / 10000;
				c = (A - a * 100000 - b * 10000) / 1000;
				d = (A - a * 100000 - b * 10000 - c * 1000) / 100;
				e = (A - a * 100000 - b * 10000 - c * 1000 - d * 100) / 10;
				f = A % 10;
				if (a * a * a * a * a * a + b * b * b * b * b * b + c * c * c * c * c * c + d * d * d * d * d * d + e * e * e * e * e * e + f * f * f * f * f * f == A)
				{
					printf("%ld\n", A);
				}
				A++;
			}
			break;
		case 7:
			A = 1000000;
			while (A < 10000000)
			{
				a = A / 1000000;
				b = (A - a * 1000000) / 100000;
				c = (A - a * 1000000 - b * 100000) / 10000;
				d = (A - a * 1000000 - b * 100000 - c * 10000) / 1000;
				e = (A - a * 1000000 - b * 100000 - c * 10000 - d * 1000) / 100;
				f = (A - a * 1000000 - b * 100000 - c * 10000 - d * 1000 - e * 100) / 10;
				g = A % 10;
				if (a * a * a * a * a * a * a + b * b * b * b * b * b * b + c * c * c * c * c * c * c + d * d * d * d * d * d * d + e * e * e * e * e * e * e + f * f * f * f * f * f * f + g * g * g * g * g * g * g == A)
				{
					printf("%ld\n", A);
				}
				A++;
			}
			break;
		}
  
	return 0;
}
```