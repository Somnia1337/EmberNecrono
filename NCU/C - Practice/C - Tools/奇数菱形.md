```c
#include <stdio.h>

int main()
{
    int n;
    int i, j, k;
    scanf("%d", &n);
    for (i = 0; i < n; i++)
    {
        j = i < n / 2 ? n / 2 - i : i - n / 2;
        for (; j > 0; j--)
            printf(" ");
        k = i < n / 2 ? i * 2 + 1 : (n - i - 1) * 2 + 1;
        for (; k > 0; k--)
            printf("*");
        puts("");
    }

    return 0;
}
```