```c
#include<stdio.h>
#include<math.h> //引入math库

int main()
{
    int x;
    int n;
    printf("输入底数：");
    scanf("%d",&x);
    printf("输入幂：");
    scanf("%d",&n);
    int a = pow(x, n); //直接调用pow幂计算函数
    printf("%d的%d次方是%d",x,n,a);
  
    return 0;
}
```