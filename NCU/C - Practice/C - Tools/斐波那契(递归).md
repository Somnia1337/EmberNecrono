```c
#include <stdio.h>

int f(int i)
{
    if (i == 0 || i == 1)
        return 1;
    return f(i - 1) + f(i - 2);
}

int main()
{
    int n, i;
    scanf("%d", &n);
    for (i = 0; i < n; i++)
    {
        printf("%d\n", f(i));
    }

    return 0;
}
```